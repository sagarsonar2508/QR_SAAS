node_modules/
.next/
# Every env file is ignored (.env, .env.local, .env.production…) except the
# committed template.
.env*
!.env.example
.pgdata/
uploads/
# drizzle/ holds hand-written SQL migrations that must deploy with the code.
# Only drizzle-kit's generated bookkeeping is ignored.
drizzle/meta/
*.log
.DS_Store
package-lock.json